{
  "name": "playwright-dev-example",
  "version": "1.0.0",
  "description": "Example test project using playwright-state-model",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "playwright-state-model": "file:.."
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "xstate": "^4.38.0"
  }
}
